# Demo mode (default) — local agents, no API cost.
AGENT_MODE=demo

# Optional: enable live Cursor SDK agents (requires API key + billing).
# Remove or comment AGENT_MODE=demo when using live agents.
# CURSOR_API_KEY=
