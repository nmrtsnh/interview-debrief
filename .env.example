# Demo mode (default) — local analysis, no API cost.
AGENT_MODE=demo

# Optional live mode — uses @cursor/sdk on the server only.
# Remove or comment AGENT_MODE=demo when enabling live mode.
# CURSOR_API_KEY must stay server-side; never expose it to the browser.
# CURSOR_API_KEY=
